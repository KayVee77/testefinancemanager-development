import { describe, it, expect } from 'vitest';

// Placeholder dashboard tests to keep suite active until component-specific
// tests are implemented or restored.
describe('Dashboard component', () => {
  it('placeholder test', () => {
    expect(true).toBe(true);
  });
});
