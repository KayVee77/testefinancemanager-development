# FinanceFlow - Production AWS Environment
# Usage: Built into Docker image via build args during CI/CD
# 
# This configuration uses:
# - AWS DynamoDB for data persistence
# - AWS Cognito for authentication (OIDC)
# - API Gateway + Lambda for backend API
# - CloudFront for CDN distribution

# ============================================================================
# ENVIRONMENT SELECTOR (REQUIRED)
# ============================================================================
# Controls auth mode and storage backend:
# - local      = localStorage + dev auth (no AWS)
# - docker     = DynamoDB Local + dev auth (Docker Compose)
# - production = DynamoDB + Cognito (AWS)
VITE_APP_ENV=production

# ============================================================================
# DISPLAY / DEBUGGING
# ============================================================================
VITE_ENVIRONMENT=production

# ============================================================================
# API CONFIGURATION
# ============================================================================
# In production, API is served via API Gateway (proxied by CloudFront)
# Format: https://xxxxxxxxxx.execute-api.eu-central-1.amazonaws.com/prod
VITE_API_BASE_URL=/api

# ============================================================================
# AWS COGNITO CONFIGURATION (REQUIRED FOR PRODUCTION)
# ============================================================================
VITE_AWS_REGION=eu-central-1
VITE_AWS_COGNITO_USER_POOL_ID=eu-central-1_kaKX5BNfr
VITE_AWS_COGNITO_CLIENT_ID=63k0okn09vas104vcjb71vhgd7
VITE_AWS_COGNITO_DOMAIN=https://eu-central-1kakx5bnfr.auth.eu-central-1.amazoncognito.com

# ============================================================================
# APPLICATION URL
# ============================================================================
# CloudFront distribution URL (HTTPS)
VITE_APP_URL=https://d1tfphpjkbqfjw.cloudfront.net

# ============================================================================
# API GATEWAY URL (SET AFTER LAMBDA DEPLOYMENT)
# ============================================================================
# If using separate API Gateway (not CloudFront proxy), set this:
# VITE_API_GATEWAY_URL=https://xxxxxxxxxx.execute-api.eu-central-1.amazonaws.com/prod
