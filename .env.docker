# FinanceFlow - Docker Environment with DynamoDB Local
# This configuration uses DynamoDB Local but keeps dev auth (no Cognito needed)

# Keep dev auth for simplicity (no Cognito)
VITE_DEV_ONLY_AUTH=true

# But enable AWS data storage mode
VITE_USE_DYNAMODB=true

# Environment name
VITE_ENVIRONMENT=docker-local

# API Gateway URL (points to dev-server which connects to DynamoDB Local)
VITE_API_GATEWAY_URL=http://localhost:3001

# AWS Configuration (for DynamoDB Local)
VITE_AWS_REGION=eu-central-1
