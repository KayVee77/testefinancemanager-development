# FinanceFlow - Docker Environment (Docker Compose)
# Usage: docker-compose -f docker-compose.unified.yml up --build
# App URL: http://localhost:8080
# 
# This configuration uses:
# - DynamoDB Local for data persistence (running in Docker)
# - Dev authentication (no Cognito needed for local testing)
# - Express backend for API + OpenAI integration

# ============================================================================
# ENVIRONMENT SELECTOR (REQUIRED)
# ============================================================================
# Controls auth mode and storage backend:
# - local      = localStorage + dev auth (no AWS)
# - docker     = DynamoDB Local + dev auth (Docker Compose)
# - production = DynamoDB + Cognito (AWS)
VITE_APP_ENV=docker

# ============================================================================
# DISPLAY / DEBUGGING
# ============================================================================
VITE_ENVIRONMENT=docker

# ============================================================================
# API CONFIGURATION
# ============================================================================
# In Docker, the API is served from the same container via nginx proxy
# /api/* routes are proxied to Express backend on port 3001
VITE_API_BASE_URL=/api

# ============================================================================
# AWS CONFIGURATION (FOR DYNAMODB LOCAL)
# ============================================================================
VITE_AWS_REGION=eu-central-1

# Note: DynamoDB endpoint is configured via docker-compose environment vars
# DYNAMODB_ENDPOINT=http://dynamodb-local:8000 (set in docker-compose.yml)
