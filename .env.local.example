# FinanceFlow - Environment Configuration (Local Development)

# Authentication Mode
# true = localStorage auth (no AWS)
# false = AWS Cognito auth
VITE_DEV_ONLY_AUTH=true

# Environment
VITE_ENVIRONMENT=local

# AI Feature - OpenAI Integration
# Local dev server URL (Node.js + OpenAI SDK)
VITE_API_BASE_URL=http://localhost:3001

# AWS Production URLs (leave empty for local dev)
# VITE_API_GATEWAY_URL=https://your-api-gateway.execute-api.eu-north-1.amazonaws.com/prod
# VITE_AWS_REGION=eu-north-1
# VITE_AWS_COGNITO_USER_POOL_ID=
# VITE_AWS_COGNITO_CLIENT_ID=
